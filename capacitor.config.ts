import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caminos.lagunita',
  appName: 'Caminos de la Lagunita',
  webDir: 'dist'
};

export default config;
